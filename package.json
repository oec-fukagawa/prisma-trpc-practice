{
  "name": "prisma-trpc-practice",
  "description": "Prisma + tRPC のプラクティスを模索するためのリポジトリ",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "functions/*",
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
